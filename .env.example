# Optional local overrides for the B00 API host.
# The host uses process-local in-memory storage and starts without external data services.
CORS_ORIGINS=http://localhost:5173
ASPNETCORE_URLS=http://localhost:8080
API_HOST_PORT=8080
WEB_HOST_PORT=5173
AI_MODE=stub
OPENAI_MODEL=
OPENAI_API_KEY=

# --- Frontend (WEB) ---
VITE_API_BASE_URL=http://localhost:8080
