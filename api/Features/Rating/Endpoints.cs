namespace TaskForge.Api.Features.Rating;

public static class RatingEndpoints
{
    public static void MapRatingEndpoints(this WebApplication app)
    {
        // Rating endpoints are called via Tasks endpoints (confirm)
    }
}
